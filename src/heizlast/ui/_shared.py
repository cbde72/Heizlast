# _shared.py is retained only as a compatibility note.
# V5.16 resolved the former wildcard import pattern by giving each mixin
# its own explicit import list. New code should not depend on this module.